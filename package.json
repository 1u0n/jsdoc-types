{
  "name": "jsdoc-types",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "lint": "eslint .",
    "tsc": "tsc --allowJs --checkJs --noEmit --target ES6 src/**/*",
    "// or if you want tsc to generate d.ts": "tsc --allowJs --checkJs --declaration --emitDeclarationOnly --outDir=types --target ES6 src/**/*",
    "check": "npm run lint && npm run tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.0.0",
    "semistandard": "^14.2.0",
    "typescript": "^3.8.3",
    "babel-eslint": "^10.1.0"
  }
}
